PORT=3000
# If you want to forward contact submissions to a Google Apps Script web app URL:
GAS_CONTACT_URL=
GAS_SUBSCRIBE_URL=
# Optional: for sending notification email on each contact (set if you want email alerts)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
NOTIFY_EMAIL=
